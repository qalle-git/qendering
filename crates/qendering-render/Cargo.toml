[package]
name = "qendering-render"
version = "0.1.0"
edition = "2021"
description = "Blender worker-pool bridge for Qendering: drives headless Blender + Sollumz to render clothing/object previews."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
